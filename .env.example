# Public site and host configuration for development
# Set this to your forwarded host (e.g. animated-fishstick-5xrvgv5r7vvh4pxp-3000.app.github.dev)
# to ensure Server Actions origin validation passes.
NEXT_PUBLIC_SITE_HOST=
# Set to 1 only for local dev if you want to allow all origins (wildcard "*")
ALLOW_ALL_ORIGINS=0

# Supabase public keys
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Database (Prisma) connection URLs
DATABASE_URL=
DIRECT_URL=

# Supabase service role (DO NOT expose publicly; keep only in server env files)
SUPABASE_SERVICE_ROLE_KEY=

# SMTP configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
